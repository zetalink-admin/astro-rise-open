---
import SimpleNewsLetter from './SimpleNewsLetter.astro';
// import more components here

// Navbar components
const components = {
  simple: SimpleNewsLetter,
};

const { type = 'simple', pageContent } = Astro.props as {
  type?: keyof typeof components;
  pageContent?: Record<string, any>;
};

// make component based on input type
const Component = components[type] || SimpleNewsLetter;
/*
How to use: 

1.add the following to the translations file in JSON format: (e.g. i18n/en/index.ts)
  [YOUR_WORD_LIST]: {
        headline: "Join the waitlist",
        sub_headline: "Enter your email address and we’ll let you know when your spot is ready",
        email_label: "Email address",
        email_form_placeholder: "Your email address",
        submit_label: "Join the waitlist",
        submit_message: "Thank you for joining the waitlist",
    }

2. import the component in the file you want to use it in (e.g. index.astro)
   import NewsLetter from "@components/sections/newsLetter/index.astro";

3. add the following to where you want to add the component
 (variable with * is optional or has default)

  Simple Hero:
   <Newsletter *type="simple" pageContent={pageContent.page.YOUR_WORD_LIST} />

  // add description here if you add new components 

*/
---

<Component pageContent={pageContent} />

---
import SimpleValuePropositions from './SimpleValuePropositions.astro';

const components = {
  simple: SimpleValuePropositions,
  // add more components here
};

const { type = 'simple', pageContent } = Astro.props as {
  type?: keyof typeof components;
  pageContent?: Record<string, any>;
};

// make component based on input type
const Component = components[type] || SimpleValuePropositions;
/*
How to use:

*/
---

<Component pageContent={pageContent} />

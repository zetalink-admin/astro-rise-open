---
import SimplePricingTable from './SimplePricingTable.astro';
// import more components here

// Navbar components
const components = {
  simple: SimplePricingTable,
};

const { type = 'simple', pageContent } = Astro.props as {
  type?: keyof typeof components;
  pageContent?: Record<string, any>;
};

// make component based on input type
const Component = components[type] || SimplePricingTable;
/*
How to use: 
1.add the following to the translations file in JSON format: (e.g. i18n/en/plans.ts)

[YOUR_WORD_LIST]:{
    plans: [
      {
        name: 'Starter',
        url: 'https://example.com/pricing/starter',
      },
      {
        name: 'Growth',
        url: 'https://example.com/pricing/growth',
      },
      {
        name: 'Enterprise',
        url: 'https://example.com/pricing/scale',
      },
    ],
    categories: [
      {
      name: 'Features',
        items: [
          {
            name: 'Edge content delivery',
            values: ['✓', '✓', '✓'],
          },
          {
            name: 'Custom domains',
            values: ['1', '3', 'Unlimited'],
          },
          {
            name: 'Team members',
            values: ['3', '20', 'Unlimited'],
          },
          {
            name: 'Single sign-on (SSO)',
            values: ['-', '-', '✓'],
          },
        ],
      },
      // add your rest of your categories and feature items here
    ],
  },

2. import the component in the file you want to use it in (e.g. index.astro)
   import PricingTable from "@components/sections/pricingTable/index.astro";

3. add the following to where you want to add the component
 (variable with * is optional or has default)

 <PricingTable *type="simple" pageContent={pageContent.page.YOUR_WORD_LIST} />

*/
---

<Component pageContent={pageContent} />

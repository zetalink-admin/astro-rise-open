---
import SimplePricingList from './SimplePricingList.astro';
// import more components here

// Navbar components
const components = {
  simple: SimplePricingList,
};

const { type = 'simple', pageContent } = Astro.props as {
  type?: keyof typeof components;
  pageContent?: Record<string, any>;
};

// make component based on input type
const Component = components[type] || SimplePricingList;
/*
How to use: 
1.add the following to the translations file in JSON format: (e.g. i18n/en/plans.ts)

[YOUR_WORD_LIST]: [
      {
      title: 'Starter',
      price: '$19',
      frequency: '/month',
      description: 'The essentials to provide your best work for clients.',
      features: ['5 products', 'Up to 1,000 subscribers', 'Basic analytics', '48-hour support response time'],
      popular: false,
      buttonVariant: 'border',
      url: '/pricing/sgtarter',
    },
    // add all of your plans here 
  ],

2. import the component in the file you want to use it in (e.g. index.astro)
   import PricingList from "@components/sections/pricingList/index.astro";

3. add the following to where you want to add the component
 (variable with * is optional or has default)

 <PricingList *type="simple" pageContent={pageContent.page.YOUR_WORD_LIST} />

*/
---

<Component pageContent={pageContent} />

---
// src/components/ui/button/index.astro
//
import Root, {
  type NavbarLayoutProps,
  type NavbarLayoutSize,
  type NavbarLayoutVariant,
  navbarLayoutVariants,
} from './navbarLayout.astro';

export {
  // Core component
  Root,
  // Export the Props type as Props
  type NavbarLayoutProps as Props,
  // Also export as alias Button
  Root as NavbarLayout,
  // Export the Tailwind Variants configuration as buttonVariants
  navbarLayoutVariants,
  type NavbarLayoutSize,
  type NavbarLayoutVariant,
};

//
---

---
// src/components/ui/button/index.astro
//
import Root, { type TemplateProps, type ButtonSize, type ButtonVariant, componentVariants } from './button.astro';

// 型エイリアスの定義
export type ButtonProps = TemplateProps;
// export type ButtonSize = TemplateProps["size"];
// export type ButtonVariant = TemplateProps["variant"];

export {
  // Core component
  Root,
  // Export the Props type as Props
  type TemplateProps as Props,
  // Also export Root as Component alias
  Root as Button,
  // Export the Tailwind Variants configuration
  componentVariants as buttonVariants,
  type ButtonSize,
  type ButtonVariant,
};

//
---

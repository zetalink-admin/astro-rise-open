---
// src/components/ui/button/index.astro
//
import Root, { type InputProps, type InputSize, type InputVariant, inputVariants } from './input.astro';

// 型エイリアスの定義
// export type InputProps = ComponentProps;
// export type ButtonSize = TemplateProps["size"];
// export type ButtonVariant = TemplateProps["variant"];

export {
  // Core component
  Root,
  // Export the Props type as Props
  type InputProps as Props,
  // Also export Root as Component alias
  Root as Input,
  // Export the Tailwind Variants configuration
  inputVariants,
  type InputSize,
  type InputVariant,
};

//
---

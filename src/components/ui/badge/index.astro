---
// src/components/ui/badge/index.astro
//
import Root, { type BadgeProps, type BadgeSize, type BadgeVariant, badgeVariants } from './badge.astro';

export {
  // Core component
  Root,
  // Export the Props type as Props
  type BadgeProps as Props,
  // Also export as component alias
  Root as Badge,
  // Export the Tailwind Variants configuration as badgeVariants
  badgeVariants,
  type BadgeSize,
  type BadgeVariant,
};

//
---

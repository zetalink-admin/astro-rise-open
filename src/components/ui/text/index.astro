---
// src/components/ui/text/index.astro
//
import Root, { type TextProps, type TextSize, type TextVariant, componentVariants } from './text.astro';

export type ButtonProps = TextProps;
// export type ButtonSize = TemplateProps["size"];
// export type ButtonVariant = TemplateProps["variant"];

export {
  // Core component
  Root,
  // Export the Props type as Props
  type TextProps as Props,
  // Also export Root as Component alias
  Root as Text,
  // Export the Tailwind Variants configuration
  componentVariants as textVariants,
  type TextSize,
  type TextVariant,
};

//
---
